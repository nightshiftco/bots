{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "slack-bot.serviceAccountName" . }}
  labels:
    {{- include "slack-bot.labels" . | nindent 4 }}
{{- end }}
